# Graco's Water Lab Workspace Rules

## Automatic Git Repository Updates
- Every time code, layout, or asset modifications are made to this project (`gracos_water_lab`), automatically stage (`git add .`), commit with a clear descriptive message, and push (`git push origin main`) to the remote GitHub repository.
